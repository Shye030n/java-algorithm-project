06/19
//변수, 자료형, if문, for문, 누적합, 연산자 복습
# 변수 : 데이터를 담는 상자
int age = 26; //정수
String name = "서현"; //문자열
double height = 165.5; //실수
boolean isWoman = true; //참거짓

int age = 25;
String name = "김서현";
double height = 160.5;
boolean beingWork = true;

=== for문
int sum = 0;

for(int i = 1; i <= 5; i++) {
   sum = sum + i;
}

System.out.println(sum);
0 1
1 2
3 3
6 4
10 5

1
2
3
4
5

0 1
1 2
3 3
6 4
===============배열
# 배열 : 같은 자료형 여러 개를 한 변수에 저장하는 것
int[] numbers = {10, 20, 30, 40, 50};
System.out.println(arr.length); //배열의 길이 출력

int[] arr = {3, 6, 9, 12};

int sum = 0;

for(int i = 0; i < arr.length; i++) {
   sum += arr[i];
}
System.out.println(sum); //30

0 3
3 6
9 9
18 12
========메서드

